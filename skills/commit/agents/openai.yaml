interface:
  display_name: "Commit"
  short_description: "Group changes into work-unit commits"
  default_prompt: "Use $commit to review the current diff, group changes by common work unit, and create separate commits."
